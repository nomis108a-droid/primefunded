rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permissive access for authenticated users to ensure administrative uploads
    // and payout proof access are not blocked.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}