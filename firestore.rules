rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive access for authenticated users to ensure administrative functions
    // (like gifting accounts or updating trader profiles) are not blocked.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}